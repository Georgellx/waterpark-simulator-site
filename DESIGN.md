# Waterpark Simulator 私有 MVP 视觉规范

- 冻结日期：2026-08-11
- 视觉目标：第 1 套“阳光水花”
- 状态：第 4 步已通过；第 5 步已实现，等待用户验收
- 设计稿：无 Figma；以 [已选择的电脑与手机效果图](docs/design/selected-homepage-direction.png) 为视觉目标

## 1. 页面要给人的感觉

网站应当让玩家在几秒内感到：明亮、好找、可信、没有百科站的负担。

它不能像赌场站、暗黑终端、通用 SaaS 模板或大型 Wiki，也不能让水上乐园图片盖过攻略入口和来源信息。

## 2. 参考方式

| 参考 | 使用方式 |
| --- | --- |
| Gamble With Your Friends | 首页骨架：简单导航、Hero、三张攻略卡、移动端折叠菜单 |
| Mina the Hollower | 可信度表达：核验日期、来源、版本范围、未知项、更新触发 |
| Farever Wiki | 快捷入口：按玩家正在解决的问题分流 |
| 已有原创 favicon | 正式品牌图形；替换效果图中的临时棕榈树 Logo |

只借结构方法，不复制参考站的颜色、字体、文案、代码、商标、图片、Logo 或图标。

## 3. 颜色

| 用途 | 色值 | 使用规则 |
| --- | --- | --- |
| 深海军蓝 | `#08263B` | Logo 底色、正文、导航、深色按钮 |
| 水蓝 | `#20C7D9` | 品牌图形、链接、信息图标和轻强调 |
| 泳池蓝 | `#0E9ED0` | 标题中的少量强调、图片叠层 |
| 深珊瑚红 | `#C8422D` | 主按钮；配白字的对比度约为 4.91:1 |
| 阳光黄 | `#FFD54A` | 小范围状态点和重点提示，不做大段正文底色 |
| 暖奶白 | `#FFF9F0` | 全站默认背景 |
| 淡水面 | `#F3FBFD` | 来源区、信息区和次级卡片背景 |
| 边框灰蓝 | `#D8E7EC` | 卡片和分隔线 |
| 次要文字 | `#526B7A` | 辅助说明；必须在实际字号下检查对比度 |

不新增紫色、霓虹绿或大面积纯黑。状态不能只靠颜色表达，必须同时有文字。

## 4. 字体与排版

- 大标题：`Bree Serif`，备用为 `Georgia, serif`。用途是保留第 1 套圆润、亲切、有游戏感的标题气质。
- 正文和界面：`Manrope`，备用为 `Arial, sans-serif`。
- 正文桌面端默认不小于 `18px`，手机端不小于 `16px`。
- 正文行宽控制在约 `65–75` 个英文字符，长文不能铺满大屏。
- H1 每页只有一个；H2、H3 按内容层级递进，不为视觉效果跳级。
- 不使用全大写长段落，不用超细字体，不用发光文字。

如果外部字体在构建时不可稳定取得，优先使用备用字体，不阻塞本地 MVP。

## 5. 首页结构

### 顶部导航

- 左侧使用已有原创 favicon，加文字 `Waterpark Simulator Guide`。
- 桌面端显示 `Guides`、`Multiplayer`、`Staff`、`Platforms`。
- 手机端显示品牌和汉堡菜单；展开后五条公开路由都能到达。
- 当前页面要有文字或下划线状态，不能只依赖颜色。

### Hero

- 桌面端采用左文右图的不对称构图，图片可延伸到右上区域。
- 首屏直接说明这是独立、经过来源核验的攻略站。
- 一个主要按钮进入 `/guides`；可有一个次要文字链接进入最重要的攻略，但不堆叠三个按钮。
- 显示 `Fan-made` 和最近核验日期。
- 主视觉使用单独生成的原创水上乐园图片或明确可用的本地素材；不得从效果图、官网或竞品截图中裁图。

### 三张攻略卡

- 只显示 Multiplayer、Staff、Console Platforms 三张卡。
- 桌面端三列，手机端单列。
- 图片、标题、一句用途说明和明确动作必须同时存在。
- 卡片高度尽量一致；说明不写未经核验的玩法承诺。

### 可信度区和页尾

- 使用淡水面背景，与主卡片区明显区分。
- 简单说明：来源类型、最近核验日期、未知项处理方式。
- 页尾明确说明独立站身份，不冒充 CayPlay 官方网站。

## 6. `/guides` 页面

- 用一句话说明三篇攻略分别解决什么问题。
- 三张入口卡直接对应三篇文章，不添加空分类、搜索框、排行榜或数据库入口。
- 每张卡显示最近核验日期和状态。
- 不复制三篇文章的完整答案，避免导航页变成重复内容页。

## 7. 三篇详情页

统一阅读顺序：

1. 面包屑。
2. H1、简短说明和直接答案。
3. `Last checked`、版本/平台范围和内容状态。
4. 正文章节；需要比较时使用清楚的表格或步骤。
5. `Unknowns`，明确哪些问题目前没有可靠答案。
6. `Sources`，列出来源名称、身份、核验日期和使用边界。
7. `Update triggers`，说明什么变化会触发复查。
8. 返回 Guides 或进入另一篇相关攻略。

Security 只作为 Staff 页中的一个章节。不得生成独立 Security、console issues、codes、tier list 或其他未批准页面。

## 8. 组件外观

- 按钮：中等圆角，不做胶囊；主按钮用深珊瑚红和白字，次按钮用深海军蓝描边。
- 卡片：白色或淡水面背景，细灰蓝边框，轻微阴影；不使用多层渐变玻璃效果。
- 攻略重点区不使用彩色粗竖条、悬浮色块或纯装饰性的强调边框；依靠标题层级、留白和内容顺序突出答案。
- 图标：使用同一套开源图标库；不使用 emoji，不手绘临时 SVG。
- 来源卡：比普通正文更规整，优先显示来源身份和边界，不显示伪造的“评分”。
- 表格：手机端允许容器内横向滚动，不能撑破整页。
- 图片：固定宽高比并预留尺寸，避免页面加载时跳动。

## 9. 交互

- 按钮 hover 只做轻微颜色变化和箭头右移，卡片最多上移 `2px`。
- 所有链接和按钮必须有清楚的键盘焦点环。
- 手机菜单可键盘操作，打开后焦点顺序合理，点击导航项后关闭。
- 尊重 `prefers-reduced-motion`；不使用自动播放视频、视差或大幅滚动动画。
- 站内链接在当前页打开；官方外链明确标识并安全打开。

## 10. 响应式规则

- 重点验收宽度：桌面 `1440px`，手机 `390px`；同时检查常见中间宽度。
- 手机端不得出现横向页面滚动。
- Hero 在手机端改为图片在上、文字在下或按效果图的可读顺序堆叠，不能把桌面端整体缩小。
- 导航、卡片、来源区和文章表格都要独立处理窄屏，不依赖字体缩小解决。
- 触控目标建议至少 `44×44px`。

## 11. 草图纠错与资产边界

- 效果图中的 `Switch` 标牌是生成错误，禁止进入网站。
- 已核验平台是 PC、PS5、Xbox Series X|S；未确认平台只能标为未列出或未知，不能画成已支持。
- 效果图中的临时 Logo、临时图标和临时句子不作为正式资产或事实。
- 使用已复制进网站的原创 favicon 与品牌图形：`app/favicon.ico`、`app/apple-icon.png`、`public/brand/site-mark.png`。
- 不热链参考站或官方宣传图片；需要图片时单独生成原创资产并保存来源记录。

## 12. 实现后的视觉验收

- 首页在电脑和手机上与选定效果图保持同一气质、层级和颜色关系。
- 玩家能在首屏找到攻略入口和核验说明。
- 五条路由的导航外观一致，三篇正文长时间阅读不疲劳。
- 无文字重叠、图片异常裁切、错误圆角、横向溢出或手机菜单遮挡。
- 焦点、对比度、图片尺寸和减少动效设置通过检查。
- 视觉验收只比较结构和表达，不要求复制效果图的生成错误。
